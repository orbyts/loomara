# Architecture

Loomara is the website assembler and application layer.

It consumes versioned artifacts from independently versioned producers. It
does not parse source Markdown on Codexa's behalf and does not absorb
producer-specific media or release pipelines.

The first supported producer will be Codexa. Future producers may provide
photography, software-release, and tutorial-media artifacts.
