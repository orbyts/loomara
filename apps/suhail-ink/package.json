{
  "name": "@orbyts/suhail-ink",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The public suhail.ink website, assembled by Loomara.",
  "scripts": {
    "build": "astro build",
    "check": "astro check",
    "dev": "astro dev",
    "preview": "astro preview",
    "test": "vitest run --passWithNoTests",
    "preview:cloudflare": "pnpm build && wrangler dev",
    "deploy:cloudflare": "pnpm build && wrangler deploy"
  },
  "dependencies": {
    "astro": "latest"
  },
  "devDependencies": {
    "@astrojs/check": "latest",
    "typescript": "latest",
    "vitest": "latest",
    "wrangler": "4.107.0"
  }
}
