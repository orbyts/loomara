{
  "name": "@orbyts/loomara-website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The first Loomara-assembled website.",
  "scripts": {
    "build": "astro build",
    "check": "astro check",
    "dev": "astro dev",
    "preview": "astro preview",
    "test": "vitest run --passWithNoTests",
    "preview:cloudflare": "pnpm build && wrangler dev",
    "deploy": "pnpm build && wrangler deploy"
  },
  "dependencies": {
    "astro": "latest"
  },
  "devDependencies": {
    "@astrojs/check": "latest",
    "typescript": "latest",
    "vitest": "latest",
    "wrangler": "4.107.0"
  }
}
